# Project Idea
The shader (kernel) language for WebGPU which I can write in Python language (translator from Python function to WGSL text). The target idea is to use it for generation of shaders for ML models;

# Project name candidates:
- py_shader_lang_wgpu
- mlsl (ml shader language)
- 

# Requirenments

- No need to have a code to run shaders, just generate shaders code;
- Extendable to other target platforms: Metal, OpenCL etc.;
- Examples for such libraries: numba, numba-cuda, Triton;

# Examples of usage:

# Steps
1) 